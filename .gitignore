# dependencies
node_modules/

# next.js / node build output
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
.vercel/

# environment files
.env
.env.local
.env*.local
!.env*.example

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# coverage
coverage/

# contracts / foundry artifacts
contracts/cache/
contracts/out/
contracts/broadcast/**
!contracts/broadcast/
!contracts/broadcast/**/
!contracts/broadcast/**/run-latest.json

# Platform-operator tooling (not part of the public protocol)
/scripts/

# Internal notes, plans, and audit logs.
# Everything operator-facing lives here so external readers can't even infer
# file names from the gitignore.
internal/

# Claude Code state
.claude/

# remotion (separate project, own tsconfig)
remotion/

# Anchor / Solana program build artifacts
solana/target/
solana/.anchor/
solana/node_modules/
solana/test-ledger/
solana/Cargo.lock

# macOS
.DS_Store
desktop.ini
.git.backup-pre-filterrepo/

# Supabase CLI local state (project ref, version pins, etc.)
supabase/
